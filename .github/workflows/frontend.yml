name: front deployment

on:
  push:
    tags:
      - 'v*'
    branches: ['dev']
    paths:
      - 'workspaces/frontend/**'
  pull_request:
    branches: [ 'dev' ]
    paths:
      - 'workspaces/frontend/**'
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}/frontend
          flavor: latest=false
          tags: |
            type=ref,suffix=-{{sha}},event=branch
            type=ref,suffix=-{{sha}},event=pr
            type=ref,event=tag

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          file: frontend.Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
  Deploy-review:
    if: github.event_name == 'pull_request'
    needs: Build
    runs-on: sandbox
    permissions:
      contents: read
      packages: write
    env:
      image: ${{ needs.Build.outputs.image }}
      external_port: 1000
      environment: review
      vite_app_dynamic_id: "a99f1241-3620-4df1-9673-96b3a3684cdf"
      vite_app_trpc_url: "https://back-review.yuki-sandbox.clivado.com/trpc"
      vite_app_iframely_id: "cd5533d79f906cb13440b5aee09e99af"
      vite_app_snapshot_url: "https://hub.snapshot.org"
      vite_app_SNAPSHOT_space: "robwalsh.eth"
      vite_app_SNAPSHOT_chain_id: 1
      vite_app_tag_id: "G-WY42TERK5P"
      vite_app_delegation_registry: "0x469788fE6E9E9681C6ebF3bF78e7Fd26Fc015446"
      vite_app_delegation_token: "0x65aFADD39029741B3b8f0756952C74678c9cEC93"
      vite_app_delegation_symbol: "USDC"
      vite_app_delegation_chain_id: 5
    defaults:
      run:
        working-directory: workspaces/frontend
    environment:
      name: review-front
      url: https://front-review.yuki-sandbox.clivado.com
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: docker image prune -f -a
      - run: docker-compose -p review up -d --wait

  Deploy-dev:
    if:  github.event_name != 'pull_request' && github.ref == 'refs/heads/dev'
    needs: Build
    runs-on: sandbox
    permissions:
      contents: read
      packages: write
    env:
      image: ${{ needs.Build.outputs.image }}
      external_port: 2000
      environment: development
    defaults:
      run:
        working-directory: workspaces/frontend
    environment:
      name: development-front
      url: https://front-dev.yuki-sandbox.clivado.com
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: docker image prune -f -a
      - run: docker-compose -p development up -d --wait

  Deploy-production:
    if: startsWith(github.event.ref, 'refs/tags/v')
    needs: [Build]
    runs-on: sandbox
    permissions:
      contents: read
      packages: write
    env:
      image: ${{ needs.Build.outputs.image }}
      external_port: 3000
      environment: production
    defaults:
      run:
        working-directory: workspaces/frontend
    environment:
      name: production-front
      url: https://front.yuki-sandbox.clivado.com
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - run: docker image prune -f -a
      - run: docker-compose -p production up -d --wait
